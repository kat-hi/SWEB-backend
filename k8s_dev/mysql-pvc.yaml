apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sweb-pv-claim-dev
  labels:
    app: sweb-pv-claim-dev
    tier: database
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 32Gi
